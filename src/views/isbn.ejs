<%- include("partials/_header") -%> <%- include("partials/_navbar") -%>

<div class="container mt-5">
  <h3 class="mt-2">Carga de ISBN</h3>
  <div class="row mt-5">
    <div class="col-md-7">
      <table id="table_id" class="display">
        <thead>
          <tr>
            <th>ISBN</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if(data) { %> <% for (var i = 0; i < data.length; i++) { %>
          <tr>
            <td><%= data[i].ISBN %></td>
            <td>
              <a
                href="isbn/delete/<%= data[i].idISBN%>"
                class="btn btn-danger btn-sm"
                ><i class="bi bi-trash"></i
              ></a>
              <a
                href="isbn/edit/<%= data[i].idISBN%>"
                class="btn btn-warning btn-sm"
                ><i class="bi bi-pencil"></i
              ></a>
            </td>
          </tr>
          <% } %> <% } %>
        </tbody>
      </table>
    </div>
    <div class="col-md-5">
      <div class="card">
        <div class="card-body">
          <form action="isbn/add" method="POST">
            <div class="form-group mt-2">
              <input
                type="text"
                name="ISBN"
                placeholder="ISBN"
                class="form-control"
              />
            </div>
            <div class="form-group mt-2">
              <button type="submit" class="btn btn-primary">GUARDAR</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include("partials/_footer") -%>
